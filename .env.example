NEXT_PUBLIC_THOR_COMMERCE_ORGANIZATION="[your-organization-id]"
BETTER_AUTH_SECRET="[openssl rand -base64 32]"
BETTER_AUTH_URL="http://localhost:3000"
# The encryption key must be 32 bytes (256 bits) and base64 encoded. You can generate a key using the following command (this is required for cloudflare workers):
NEXT_SERVER_ACTIONS_ENCRYPTION_KEY="[openssl rand -base64 32]"